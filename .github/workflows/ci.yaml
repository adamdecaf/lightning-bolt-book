name: CI

on:
  pull_request:
  push:
    branches: [master]

permissions:
  contents: read

jobs:
  epub:
    name: EPUB
    runs-on: macos-latest
    steps:
      - name: Check out code
        uses: actions/checkout@v7

      - name: Install pandoc
        run: brew install pandoc

      - name: Download BOLTs
        run: make setup

      - name: Build EPUB
        run: make epub

      - name: Upload EPUB
        uses: actions/upload-artifact@v4
        with:
          name: lightning-bolt-book.epub
          path: lightning-bolt-book.epub

  pdf:
    name: PDF
    runs-on: macos-latest
    steps:
      - name: Check out code
        uses: actions/checkout@v7

      - name: Install dependencies
        run: brew install pandoc weasyprint

      - name: Download BOLTs
        run: make setup

      - name: Build PDF
        timeout-minutes: 10
        run: make pdf

      - name: Upload PDF
        uses: actions/upload-artifact@v4
        with:
          name: lightning-bolt-book.pdf
          path: lightning-bolt-book.pdf

  html:
    name: HTML
    runs-on: macos-latest
    steps:
      - name: Check out code
        uses: actions/checkout@v7

      - name: Install pandoc
        run: brew install pandoc

      - name: Download BOLTs
        run: make setup

      - name: Build HTML
        run: make html

      - name: Require outputs
        run: |
          test -s docs/book/index.html
          test -s docs/book/bolt-00.html
          test -s docs/book/introduction.html

      - name: Upload HTML
        uses: actions/upload-artifact@v4
        with:
          name: lightning-bolt-book-html
          path: docs/book
